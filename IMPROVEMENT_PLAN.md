## План улучшений Comfort CRM (Frontend)

### Цели
- Ускорить первый рендер и навигацию (FP/FCP, TTI)
- Снизить нагрузку на DOM и количество перерисовок
- Упростить архитектуру, уменьшить дублирование кода
- Повысить надёжность UI и качество ошибок

### Что уже сделано
- Вынесены иконки в `frontend/src/components/ui/Icons.js`
- Общая модалка `frontend/src/components/ui/Modal.js` (портал, Esc/overlay, быстрые анимации)
- Ленивая выдача карточек на дашборде (сначала 9, далее по 9 через `IntersectionObserver`)
- Исправлены импорты и падения (Balance/Expense/AddClient/ClientDetails)

### Быстрые победы (1–2 дня)
- Использовать `useMemo` для фильтрации клиентов на дашборде и мемоизировать карточку клиента (`React.memo`)
- Заменить блокирующие `alert` на единый `NotificationToast` (уже есть компонент)
- Единый axios-инстанс `apiClient` с интерсептором авторизации (заменить ручной вызов `getAuthHeaders` там, где очевидно)
- Скелетоны вместо спиннеров в `Dashboard`, `ClientDetails` и модалках (перцептивное ускорение)

### Архитектура кода
- UI-компоненты
  - Вынести повторяющиеся элементы в `frontend/src/components/ui/`:
- `Button`, `Skeleton`, `Card`, `ProgressBar`
    - централизованные классы Tailwind для一致ности
  - Карточка клиента → `components/ClientCard.jsx` (мемо-компонент)
  - Статус платежа → `components/PaymentStatusBadge.jsx`

- Слой данных
  - `frontend/src/lib/apiClient.js`: `axios.create({ baseURL: API })`, интерсепторы 401/сетевые повторы
  - `frontend/src/lib/services/`: функции: `dashboardService.fetch(capitalId)`, `clientsService.updatePaymentStatus(...)`
  - Константы/типы: `frontend/src/lib/types.js` (JSDoc типы сущностей)

### Производительность
- Списки
  - Уже: ленивый рендер (9+); добавить кнопку «Загрузить ещё» как fallback — уже есть
  - При >300 карточек рассмотреть виртуализацию (react-window)
- Рендер
  - `useMemo`/`useCallback` в местах со сложной фильтрацией и расчётами
  - Мемоизация `PaymentProgress`
  - Избегать анонимных функций в JSX там, где это создаёт лишние diff
- Стили
  - Проверить тяжёлые backdrop-blur/box-shadow на дешёвые альтернативы в списках

### Сеть и данные
- HTTP-кеш на бэке: ETag/Last-Modified для дашборда/справочников
- Импорт клиентов
  - Ограничение параллельности при массовой отправке (пакетами по 3–5)
  - (Опционально) перенос парсинга XLSX в Web Worker для больших файлов

### UX / UI
- Скелетоны вместо спиннеров
- Единый модальный стиль через `Modal`
- Улучшение доступности: роли/aria-атрибуты у интерактивных элементов

### Надёжность и ошибки
- Единый хендлер ошибок (toaster + лог в консоль)
- Нестрогие зоны заменить на понятные сообщения пользователю (импорт, сеть)

### Тесты и качество
- Unit-тесты утилит/сервисов (Jest)
- Лёгкие smoke-тесты рендера ключевых страниц (React Testing Library)
- Линт «на предупреждения» в CI, форматирование Prettier

### Итерации внедрения
- Этап 1 (сегодня–завтра)
  - `useMemo` фильтра дашборда, `ClientCard` + `React.memo`
  - Toast вместо `alert` в модалках
  - Скелетоны на `Dashboard`/`ClientDetails`
- Этап 2
  - `apiClient` + сервисы, постепенная замена прямых axios вызовов
  - UI-компоненты (`Button`, `Card`, `ProgressBar`)
- Этап 3
  - Виртуализация списка при больших объёмах
  - Web Worker для импорта (по необходимости)

### Метрики успеха
- FCP/TTI на дашборде (цель < 1.2s локально)
- Уменьшение узла DOM на первичном рендере (>50%)
- Отсутствие «jank» при открытии/закрытии модалок (<150ms)

### ToDo чек-лист
- [ ] `useMemo` в `Dashboard` и мемо `ClientCard`
- [ ] `NotificationToast` вместо `alert`
- [ ] `apiClient` + сервисы
- [ ] `Skeleton` компоненты и применение
- [ ] Виртуализация списка (при необходимости)

